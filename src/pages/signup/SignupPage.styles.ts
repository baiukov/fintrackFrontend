import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	form: {
		gap: 80,
	},

	textFields: {
		display: 'flex',
		alignItems: 'center',
		gap: 20,
	},

	orText: {
		color: '#fff',
		opacity: 0.5,
		fontSize: 12,
		fontWeight: '800',
		textAlign: 'center',
		marginBottom: 10,
		textTransform: 'uppercase',
		fontStyle: 'italic',
	},
})
