import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	form: {
		gap: 120,
	},

	textFields: {
		display: 'flex',
		alignItems: 'center',
		gap: 20,
	},

	forgotPassword: {
		fontSize: 16,
		color: '#0645AD',
	},
})
