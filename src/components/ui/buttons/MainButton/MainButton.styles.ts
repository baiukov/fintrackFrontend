import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	button: {
		width: 900,
		height: 120,
		textAlign: 'center',
		borderRadius: 40,
	},

	primary: {
		backgroundColor: '#3D4CC9',
	},

	secondary: {
		borderColor: 'FFFFFF',
		borderWidth: 5,
	},
})
