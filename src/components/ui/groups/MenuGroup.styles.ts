import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	title: {
		color: 'white',
		fontSize: 30,
		fontWeight: 'bold',
		marginBottom: 20,
	},

	items: {
		marginLeft: 25,
		gap: 5,
	},
})
