import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	title: {
		color: 'white',
		fontSize: 26,
		fontWeight: 'bold',
		marginBottom: 10,
		alignSelf: 'center',
	},

	items: {
		gap: 5,
		marginBottom: 30,
	},
})
