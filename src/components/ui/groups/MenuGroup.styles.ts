import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
	title: {
		color: 'white',
		fontSize: 30,
		fontWeight: 'bold',
		marginBottom: 10,
		alignSelf: 'center',
	},

	items: {
		gap: 5,
	},
})
